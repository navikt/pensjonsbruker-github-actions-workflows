name: Deploy alerts
on:
  workflow_call:
    inputs:
      nais-template-q2:
        required: false
        type: string
        default: ""
      nais-template-production:
        required: true
        type: string
      dev-cluster:
        required: false
        type: string
        default: ""
      production-cluster:
        required: true
        type: string

jobs:
  deploy-q2-alerts:
    if: ${{ inputs.nais-template-q2 != '' && inputs.dev-cluster != '' }}
    name: Deploy alerts q2
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Deploy alerts
        uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: ${{ inputs.dev-cluster }}
          RESOURCE: ${{ inputs.nais-template-q2 }}

  deploy-prod-alerts:
    if: github.ref == 'refs/heads/main'
    name: Deploy alerts prod
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Deploy alerts
        uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: ${{ inputs.production-cluster }}
          RESOURCE: ${{ inputs.nais-template-production }}
